const add = function (a, b) {
    return a + b;
}

const sub = (a, b) => a - b;

const mul = (a, b) => a * b;
const div = (a, b) => a / b;
const murali_arith = { add, sub, mul, div }

module.exports = murali_arith;

